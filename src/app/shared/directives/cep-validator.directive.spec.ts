import { CepValidatorDirective } from './cep-validator.directive';

describe('CepValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CepValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
